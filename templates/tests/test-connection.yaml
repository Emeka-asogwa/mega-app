apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mega-app.fullname" . }}-test-connection"
  labels:
    {{- include "mega-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mega-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
